# Copy this to .env.local and fill with your Supabase Postgres connection

# Recommended: pooled connection string (works best with serverless)
# Find in Supabase Dashboard → Settings → Database → Connection string → Pooling
# Example:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@<ref>.pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=
